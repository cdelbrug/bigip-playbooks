Some generic playbook roles for creating a new VIP or adding sites to an existing VIP.

new-vip.yml - Create your new VIPs using LTM policy to forward based on host header to pools
new-site.yml - Create and add pools to existing VIP LTM policy and create related GTM configuration
